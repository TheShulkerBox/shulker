"""
Use this file to fuck around :D
"""

from lib:xp import XP


class tacos(base_item):
    id = "minecraft:stone_sword"
    item_name = "taco sauce"
    lore = ["taco", "sauce"]
    weapon = {damage: 20}

class op_sword(weapon_item):
    id = "minecraft:stone_sword"
    item_name = "taco sauce"
    lore = ["taco", "sauce"]
    weapon = {damage: 20}


class op_helmet(armor_item):
    id = "minecraft:diamond_helmet"
    item_name = "matador"
    lore = ["taco", "sauce"]


class mace(weapon_item):
    id = "iron_shovel"
    item_model = "mace"
    item_name = "Iron Mace"
    lore = ["Stun active - crit to stun enemy"]
    enchantment_glint_override = true
    weapon = {damage: 7.5, speed: 0.7}
    custom_data = {stun: true}



function taco:sauce:
    if items entity @s weapon.mainhand (-op_helmet):
        return run say i am holding an op_helmet
    say i am not holding a op_helmet

from lib:db import PlayerDB

function a:b:
    for t in PlayerDB:
        TEMP["tacos"] = t.data
        if TEMP["tacos"] > 1:
            say hi
