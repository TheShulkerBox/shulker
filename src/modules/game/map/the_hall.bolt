from ../models import Map, Team, MapTag

market = Map(
    id="the_hall",
    tags=[
        MapTag.Deathmatch,
        MapTag.All,
    ],
    teams=[
        Team(id="deathmatch_1", color="dark_gray", spawn=(1000, 63, 4016, 180, 0)),
        Team(id="deathmatch_2", color="dark_gray", spawn=(1000, 63, 3984, 0, 0)),
    ],
    location=(1000, 63, 4000),
    dimension="overworld",
    max_player_count=2
)