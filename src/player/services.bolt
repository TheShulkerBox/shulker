from server:core import ROOT_ADVANCEMENT
from server:events import ON_END_TICK


PLAYER_TICK = ~/tick


append function ON_END_TICK:
    as @a as @s run function PLAYER_TICK
    advancement revoke @a through ROOT_ADVANCEMENT
