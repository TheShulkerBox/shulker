from ../base import Item


class Rations(Item):
    """"""
    id = "dried_kelp"
    consumable = {
        consume_seconds: 0.8,
        on_consume_effects: [
            {
                type: "apply_effects",
                effects: [
                    {
                        id: "regeneration",
                        amplifier: 3,
                        duration: 3 * 20,
                    }
                ]
            }
        ]
    }
