"""
This handles sysadmin related things such as:
- on server startup

Functions:
- `sys:tick` runs every tick
- `sys:load` runs on load
- `sys:tick/players` run for every player (ordered last in the tick cycle)
"""

from functools import cache

from bolt_expressions import Scoreboard, Expression

from lib:text import Theme, boxed_text

#> Function Paths
SERVER_LOAD = ./load
SERVER_TICK = ./tick
SPAWN_ENTITIES_LOADED = ./load/spawn_entities

#> Adv Paths
ROOT_ADVANCEMENT = ./root

#> Globals Scoreboards
SERVER_GLOBALS = Scoreboard("shulker.server.globals", criteria="dummy")


# @cache
# def clock(speed: str):
#     """
#     Return a shared clock function.

#     Summit invokes the 1t clock through the booth's ``persistent_tick``. Slower
#     clocks remain scheduled clocks and should only be used for short-lived work,
#     per the Summit handbook.

#     ```
#     function clock("4t"):
#         say every 4 ticks
#     ```
#     """

#     if (speed == "1t") or (speed == "1"):
#         return SERVER_TICK

#     path = f"{SERVER_TICK}/{speed}"
    
#     append function SERVER_LOAD:
#         schedule function path speed replace

#     function path:
#         schedule function path speed replace
    
#     return path


prepend function_tag minecraft:load { "values": [SERVER_LOAD] }

function SERVER_LOAD:
    # forceload add 0 0
    # tellraw @a[tag=staff] boxed_text(
    #     {
    #         text: "The Shulker Box",
    #         hover_event: {
    #             action: "show_text",
    #             value: {text: "By rx97, Tallone55, and friends", color: "gray"},
    #         },
    #     },
    #     text_color=Theme.Primary,
    #     box_color=Theme.Secondary,
    # )
    Expression.init()
    # schedule function ./wait_for_spawn_loaded 5t replace


# function ./wait_for_spawn_loaded:
#     execute if loaded ~ ~ ~ run return run function SPAWN_ENTITIES_LOADED
#     schedule function ./wait_for_spawn_loaded 5t replace


# Cache the Summit-owned persistent clock path. Unlike slower burst clocks, this
# must not schedule itself: Summit calls it through ``persistent_tick``.
# clock("1t")


advancement ROOT_ADVANCEMENT { "criteria": { "impossible": { "trigger": "minecraft:impossible" } } }
