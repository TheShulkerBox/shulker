from server:core import server_load

DUELS_LOBBY_TEAM = "duels.lobby"

# Stores what map id the player is apart of
DUELS_MAP_ID = Scoreboard("duels.map_id")

append function server_load:
    team add DUELS_LOBBY_TEAM "Duels Lobby:"
