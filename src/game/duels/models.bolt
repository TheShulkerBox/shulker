from ../models import Map, Gamemode


class DuelsMap(Map):
    gamemode: str = Gamemode.Duels

    location: str
    spawn_1: str
    spawn_2: str
    dimension: str = "overworld"
    max_player_count: int = 2


macro duels_map path=brigadier:string data=mecha:nested_json:
    DuelsMap.generate(id=path, data=data)
